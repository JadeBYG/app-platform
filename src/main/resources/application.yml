spring:
  datasource:
    url: jdbc:mysql://localhost:3307/app_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: app
    password: app123

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8080
